@using HotelDruid.Client.Localization
@using Microsoft.AspNetCore.Components
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResource> L

<div class="drawer-shell">
    <div class="drawer-title">@L["Nav.Toggle", "Navigation menu"]</div>
    <div class="nav-scrollable" @onclick="HandleNavigate">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                @L["Nav.Home", "Home"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="assets">
                @L["Nav.Assets", "Assets"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="warehouses">
                @L["Nav.Warehouses", "Warehouses"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="inventory">
                @L["Nav.Inventory", "Inventory"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;">@L["Nav.Section.GuestManagement", "Guest Management"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="clients">
                @L["Nav.Clients", "Clients"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="client-data">
                @L["Nav.ClientData", "Client Data"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;">@L["Nav.Section.ReferenceData", "Reference Data"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="nations">
                @L["Nav.Nations", "Nations"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="regions">
                @L["Nav.Regions", "Regions"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cities">
                @L["Nav.Cities", "Cities"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="identity-document-types">
                @L["Nav.IdentityDocumentTypes", "ID Document Types"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="family-relationships">
                @L["Nav.FamilyRelationships", "Family Relationships"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;">@L["Nav.Section.Configuration", "Configuration"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cash-registers">
                @L["Nav.CashRegisters", "Cash Registers"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="user-groups">
                @L["Nav.UserGroups", "User Groups"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="users">
                @L["Nav.Users", "Users"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="settings">
                @L["Nav.Settings", "Settings"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="setup">
                @L["Nav.Setup", "Setup"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;">@L["Nav.Section.UserManagement", "User Management"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="user-privileges">
                @L["Nav.UserPrivileges", "User Privileges"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="user-relations">
                @L["Nav.UserPreferences", "User Preferences"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="group-memberships">
                @L["Nav.GroupMemberships", "Group Memberships"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-weight:600;letter-spacing:.05em">@L["Nav.Section.YearOperations", "YEAR OPERATIONS"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="years">
                @L["Nav.Years", "Years"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="periods">
                @L["Nav.Periods", "Periods"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="tariffs">
                @L["Nav.Tariffs", "Tariffs"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="assignment-rules">
                @L["Nav.AssignmentRules", "Assignment Rules"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-weight:600;letter-spacing:.05em">@L["Nav.Section.BookingsFinance", "BOOKINGS &amp; FINANCE"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="bookings">
                @L["Nav.Bookings", "Bookings"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="booking-costs">
                @L["Nav.BookingCosts", "Booking Costs"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="booking-guests">
                @L["Nav.BookingGuests", "Booking Guests"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cancelled-bookings">
                @L["Nav.CancelledBookings", "Cancelled Bookings"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="expenses">
                @L["Nav.Expenses", "Expenses"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="money-history">
                @L["Nav.MoneyHistory", "Money History"]
            </NavLink>
        </div>
        <div class="nav-item px-3 mt-2">
            <div class="text-muted small px-1" style="font-weight:600;letter-spacing:.05em">@L["Nav.Section.Communication", "COMMUNICATION"]</div>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="messages">
                @L["Nav.Messages", "Messages"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="contract-templates">
                @L["Nav.ContractTemplates", "Contract Templates"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="external-integrations">
                @L["Nav.ExternalIntegrations", "External Integrations"]
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="sessions">
                @L["Nav.Sessions", "Sessions"]
            </NavLink>
        </div>
    </nav>
    </div>
</div>

@code {
    [Parameter]
    public EventCallback OnNavigate { get; set; }

    private Task HandleNavigate()
    {
        return OnNavigate.HasDelegate ? OnNavigate.InvokeAsync() : Task.CompletedTask;
    }
}

